#include <stdlib.h>
#include <iostream>
#include <vector>
#include <string> 

using namespace std;

class Solution
{
public:
	bool isScramble(string& s1, string& s2) {


	}
	
	bool helper(string& s1, int pos, int sub_len, string target) {
		if (target == s1) return true;
		// i -> substr len
		for(int i=1; i<pos; ++i) {
			helper(s1, )
			string sub1 = s.substr(pos-i, i), sub2 = s.substr(pos, i);
			string tmp = 



		}


	}
};